---
# this profile is well-defined, however the launch parameters
# it provides to `sslocal` are insufficient, therefore
# the profile will be loaded, but `sslocal` will fail to launch

display_name: "Does not launch"
# we do not provide `sslocal` with sufficient launch parameters
#config_path: "./ss.json"
#extra_args:
#  - "--tcp-fast-open"
