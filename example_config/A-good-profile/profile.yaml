---
# this is an example of a good profile with good `sslocal` launch parameters

display_name: "Example Profile" # optional, defaults to current directory's name
pwd: "." # optional, defaults to `.` (current directory of this YAML file)
sslocal_path: "sslocal" # optional, defaults to `sslocal` (performs lookup in `$PATH`)
ss_config_path: "./ss.json" # optional, defaults to `None`
extra_args: # optional, a list of tokens passed to `sslocal`, defaults to `[]` (empty)
  - "--tcp-fast-open"
  - "--worker-threads"
  - "4"
